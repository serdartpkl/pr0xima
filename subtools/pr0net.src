import_code("../pr0xima/init.src")

prx = new Proxima

targetESSID = null
if params.hasIndex(0) then targetESSID = params[0]

prx.globals.crypto = include_lib("/lib/crypto.so")

prx.net.autoHack(targetESSID)